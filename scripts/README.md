# Scripts — OMAR OS

> **Placeholder (v0.1).** Intentionally minimal.

Reserved for executable helpers (project scaffolding, validation, link-checking,
knowledge promotion) as the system gains software (CORE v0.2+).

## Current state

- No scripts yet. v0.1 is documentation-first; the spec does not require code.
- When scripts are added, each should be small, testable, local-first, and replaceable
  (constitution / ROADMAP Phase 1 philosophy).

## Suggested future scripts (not yet built)
- `new-project` — scaffold a project from `projects/_template/`.
- `check-links` — verify Markdown links across the repo.
- `validate` — check structural consistency (templates, ADR numbering).

## See also
- Roadmap: [`../ROADMAP.md`](../ROADMAP.md).
